#ifndef METRIC_HEADER
#define METRIC_HEADER

template <typename Numeric, typename Categorical> struct RMSE_ACC{
  
};

template <typename Numeric, typename Categorical> struct MAE_ACC{
  
};













#endif
